// Preview UI theme tokens for trim + scrubber visuals.
// Keep these as CSS custom-property strings so themes can override them.

// Theme-driven CSS var string; expected to remain CSS rather than resolved colors.
export const TRIM_TRACK_BASE_COLOR = "var(--trim-track-base, var(--surface-5))";
export const TRIM_HIGHLIGHT_ACTIVE = "var(--trim-highlight-active)";
export const TRIM_HIGHLIGHT_IDLE = "var(--trim-highlight-idle)";
